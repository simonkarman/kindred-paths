{
  "name": "kindred-paths",
  "version": "0.0.1",
  "author": "Simon Karman",
  "description": "Shared code for Kindred Paths, the custom Magic the Gathering set by Simon Karman",
  "main": "dist/src/index.js",
  "private": true,
  "scripts": {
    "precommit": "npm run clean && npm run lint && npm run build",
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint --ext .ts",
    "dev": "npm run build -- --watch",
    "test": "ts-jest --coverage --silent --verbose"
  },
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "@typescript-eslint/eslint-plugin": "^5.57.1",
    "@typescript-eslint/parser": "^5.57.1",
    "eslint": "^7.32.0",
    "eslint-plugin-import": "^2.24.2",
    "eslint-plugin-jest": "^24.4.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.2"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}

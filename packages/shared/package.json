{
  "name": "@kindred-paths/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Isomorphic domain: card model, search DSL, hashing, colors, typography, layouts. Populated by porting from v1 shared/ in Phase 1b.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}

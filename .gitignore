collection
node_modules
.envrc

# Build outputs from v2 workspace packages (packages/*/dist/)
packages/*/dist/

# Render cache (KP_CACHE_DIR default) — content-hashed PNGs + thumbnails, not source.
.cache/
